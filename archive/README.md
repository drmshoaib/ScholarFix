# Archive

This folder keeps legacy or placeholder files that are no longer part of the
canonical app path.

- `app2_legacy.py` is an older Streamlit prototype.
- `tex_parser_placeholder.py` was an empty root-level placeholder. The active
  LaTeX parser is `parsers/tex_parser.py`.
